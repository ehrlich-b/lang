// expect: 0
// Pointer operations test
include "std/core.lang"

func main() i64 {
    var x i64 = 42;
    var p *i64 = &x;

    if *p > 0 && *p < 100 {
        *p = *p + 1;
    }

    // Logical operators
    var a bool = true;
    var b bool = false;

    if a || b {
        if !b {
            println("logic ok");
        }
    }

    // Comparison operators
    if x != 0 && x >= 1 {
        return 0;
    }
    return 1;
}
