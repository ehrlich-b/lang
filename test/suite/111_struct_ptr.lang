// expect: 142
// Struct field access through pointer
include "std/core.lang"

struct Point {
    x i64;
    y i64;
}

func main() i64 {
    var p *Point = alloc(16);
    p.x = 42;
    p.y = 100;
    return p.x + p.y;
}
