// expect: 0
// Logical: chained operators
func main() i64 {
    // (1 && 1) && 1 = true
    if 1 && 1 && 1 {
        // (0 || 0) || 1 = true
        if 0 || 0 || 1 {
            // (1 && 0) || 1 = true
            if 1 && 0 || 1 {
                return 0;
            }
        }
    }
    return 1;
}
