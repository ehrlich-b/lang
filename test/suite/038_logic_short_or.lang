// expect: 0
// Logical: OR short-circuit (right side not evaluated when left is true)
func main() i64 {
    var x i64 = 0;
    // If short-circuit works, x should stay 0
    if 1 || (x = 1) {
        if x == 0 { return 0; }
        return 1;  // fail: x was modified
    }
    return 2;  // fail: should have entered
}
