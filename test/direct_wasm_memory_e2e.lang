var text *u8 = "lang";

func checksum(s *u8) i64 {
    var total i64 = 0;
    while *s != 0 {
        total = total + *s;
        s = s + 1;
    }
    return total;
}

func main() i64 {
    *(text + 1) = 111;
    return checksum(text) % 256;
}
